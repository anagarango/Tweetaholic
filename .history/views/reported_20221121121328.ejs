<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .postBox{
          border:black solid 1px;
          border-radius: 10px;
          padding: 15px;
          margin-bottom: 20px;
        }

        .report{
          background-color: #D34A4A;
          color:white;
          padding: 10px;
          border-radius: 15px;
        }

        .reported{
          color:red
        }
    </style>
</head>

<body>
    <h1>These posts have been reported, please review if they must be deleted</h1>
  <% reportPosts.forEach(reportPosts => { %>
    <div class="postBox">
      <h2><%= reportPosts.title %></h2>
      <p><%= reportPosts.body %></p>
      <p><%= reportPosts.time %></p>
      <form action="post/delete/<%= reportPosts.id %>" method="post">
        <button class="report" type="submit">Delete</button>
      </form>
    </div>

  <% }) %>

  <div>
    <h2 class="reported"></h2>
  </div>
 
  
  



   
</body>

</html>